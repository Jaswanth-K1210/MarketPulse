# Backend API Configuration
# Update these URLs if your backend runs on a different host/port

# REST API endpoint
VITE_API_URL=http://localhost:8000

# WebSocket endpoint for real-time updates
VITE_WS_URL=ws://localhost:8000/ws
